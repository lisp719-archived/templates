swagger: "2.0"
info:
  version: "1.0.0"
  title: minimal
paths:
  /users:
    get:
      responses:
        "200":
          description:  Describe the 200 response in more detail
          schema:
            $ref: "#/definitions/User"
definitions:
  User:
    properties:
      id:
        type: integer
